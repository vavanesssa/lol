// src/theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme( {
  palette: {
    primary: {
      main: '#000', // Replace with your desired primary color
    },
    secondary: {
      main: '#000', // Replace with your desired secondary color
    },
  },
} );

export default theme;
